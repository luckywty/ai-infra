---
tags: [ai-infra, 学习计划, 推理引擎, 求职]
type: 学习计划
status: 进行中
period: 第2~10周
reviewed: 2026-08-03
topic: 大模型推理引擎第 2—10 周学习计划
keywords:
  - vLLM
  - KV Cache
  - GQA
  - CUDA Softmax
  - Token Budget/
  - 求职准备
summary: 9 周学习计划：Gate 0 环境准入、vLLM Serving 基线、CUDA Kernel 与 Profiler 闭环、引擎源码改动与回归、面试验收与止损规则，目标形成通过实习筛选的最小证据链。
---

> [!info] 计划定位
>
> 计划假设：熟悉 C++ / Python，有少量 CUDA 经验；剩余 9 周，每周可投入 20—25 小时。

# 大模型推理引擎：第 2—10 周学习计划

## 统一时间预算

每周预排 **20 小时核心任务**，另保留 **0—5 小时故障/加分缓冲**：

| 模块 | 每周时间 |
|---|---:|
| 本周主线技术与编码 | 11 小时 |
| 测试、Benchmark、报告 | 4 小时 |
| 算法现场编码 | 3 小时 |
| C++ / OS / 并发 / 网络 | 2 小时 |

缓冲时间不能预先分配。核心任务没有完成时，禁止开始加分项。

## 统一作品结构

只维护一个作品集仓库和一个 vLLM fork / patch：

```text
llm-inference-systems-lab/
├── environment/
├── mini_inference/
├── serving/
│   ├── workloads/
│   ├── scripts/
│   ├── configs/
│   ├── raw_results/
│   └── analysis/
├── cuda_softmax/
│   ├── src/
│   ├── tests/
│   ├── benchmarks/
│   └── profiler/
├── engine_patch/
│   ├── patch/
│   ├── tests/
│   └── trace_analysis/
├── regression/
└── README.md
```

## Gate 0：第 2 周开始时先做环境准入（2—3 小时）

在 Gate 0 通过前，不启动后续正式实验。不得假设已有任意型号 GPU、双卡、Nsight 权限或可编译 vLLM。

必须固定并保存：

- 原生 Linux 或 WSL2 版本；
- GPU 型号、显存、驱动；
- Python、PyTorch、CUDA Toolkit、Triton；
- 一个可以安装、启动服务并运行目标测试的 vLLM tag / commit；
- 模型 ID、模型 revision、Tokenizer revision、dtype、`max_model_len`；
- 可用磁盘空间和模型缓存路径。

硬件适配：

| 显存 | 起始模型建议 |
|---:|---|
| 8—12 GB | 0.5B—1.5B；降低上下文和并发 |
| 16—24 GB | 1.5B—3B；先核算 KV Cache |
| 40 GB 及以上 | 可考虑 7B BF16，但不以模型大为目标 |

必须执行的准入检查：

```text
nvidia-smi
→ PyTorch CUDA smoke test
→ nvcc / CMake smoke test
→ vLLM 离线最小推理
→ vLLM 在线服务最小请求
→ compute-sanitizer
→ nsys
→ ncu 硬件计数器权限
```

失败分支：

- `ncu` 权限不可用：第 6 周改用 CUDA Event、Nsight Systems 和 PyTorch Profiler，不因缺少硬件计数器停摆；
- vLLM 源码完整编译失败：固定官方可用 wheel 验证服务，另用同 commit 的 Python editable workflow 做 Python 层 patch；
- 只有单卡：删除多卡实测；
- `stable` 文档与固定 commit 不一致：以固定 commit 的源码、测试和对应版本文档为准，`stable` 页面只作入口。

输出：

```text
environment/
├── environment.md
├── collect_env.txt
├── versions.lock
├── hardware.md
└── smoke_test.md
```

---

## 第 2 周：推理机制与 C++ 能力诊断

[[DAY01 推理数据流与 Attention Shape]]

[[DAY02 单层 Causal GQA Attention]]

[[DAY03 无 KV Cache 的自回归 Decode]]

[[DAY04 KV Cache 数据结构与 Prefill]]

> [!note] 目标
>
> - 理解 GQA、Prefill、Decode、KV Cache；
> - 验证自己是否真的具备后续所需的 C++ 基础。

> [!info] 学习资料
>
> - [CS336](https://cs336.stanford.edu/)：Transformer / Attention / Inference 相关部分；
> - [Hugging Face LLM inference optimization](https://huggingface.co/docs/transformers/llm_optims)；
> - cppreference：RAII、移动语义、智能指针、容器。

> [!example] 11 小时主线（含 Gate 0）
>
> 1. 用 2—3 小时完成 Gate 0；
> 2. 实现单层 causal GQA Attention；
> 3. 实现有/无 KV Cache 两条 Decode 路径；
> 4. 手算并验证逻辑 KV Cache 容量；
> 5. 在本周 2 小时 C++ / 系统时间内完成一个小型 RAII 资源封装和单元测试。

> [!example] 4 小时测试与报告
>
> - `pytest` 逐 Decode step 比较有/无 Cache 的 hidden output 或 logits，而不只比较最后一步；
> - 覆盖 batch 内不同有效长度、非零 position offset、GQA head 映射和 Cache append 边界；
> - FP32 使用严格容差，FP16 单独设置并解释较宽容差；
> - 输出 Prefill / Decode / KV 数据流图；
> - 5 分钟口述：一个 Token 如何进入 Attention 并写入 KV Cache。

> [!success] 达标标准
>
> - Cache 与无 Cache 最后位置输出在容差内一致；
> - 能手算 KV Cache；
> - C++ 代码无裸资源泄漏，能解释拷贝、移动和析构；
> - 未达标时，第 3—4 周每周从缓冲中增加 2 小时 C++，不能假设自己“熟悉 C++”。

---

## 第 3 周：vLLM Serving 与规范基线

> [!note] 目标
>
> 建立可复现的在线 Serving 基线，而不是只会启动服务。

> [!info] 学习资料
>
> - [vLLM V1 Guide](https://docs.vllm.ai/en/stable/usage/v1_guide/)
> - [vLLM Architecture Overview](https://docs.vllm.ai/en/stable/design/arch_overview/)
> - [`vllm bench serve`](https://docs.vllm.ai/en/stable/cli/bench/serve/)
> - [vLLM Production Metrics](https://docs.vllm.ai/en/stable/usage/metrics/)

> [!example] 11 小时主线
>
> 1. 固定 vLLM commit、模型 revision、dtype、硬件；
> 2. 跑通 OpenAI-compatible Streaming 服务；
> 3. 用官方工具完成开放环基线；
> 4. 保存 TTFT、TPOT、E2E、Goodput、失败率；
> 5. 抓取 running / waiting requests、KV Cache usage 和阶段时间；
> 6. 写一个最薄的异步客户端交叉验证 TTFT，不重复实现完整压测框架。

> [!example] 4 小时测试与报告
>
> - 预热和正式实验分离；
> - 固定 seed 和采样参数；
> - 客户端与官方工具的小负载结果差异有解释；
> - 在满足 `environment.md` 所列系统前置条件的机器上，能够从 fresh clone 通过单一 setup 流程复现；不声称仓库可以安装 GPU 驱动等系统依赖。

> [!success] 达标标准
>
> - 能解释开放环与固定并发的区别；
> - 能解释 TTFT / TPOT / Goodput；
> - 请求失败和超时不被静默删除；
> - README 中没有手填性能数字。

---

## 第 4 周：Serving 主项目 v1

> [!note] 目标
>
> 回答一个问题：
>
> > Mixed workload 接近饱和时，Token Budget 如何影响 TTFT、TPOT 和 SLO Goodput？

> [!example] 11 小时主线
>
> 1. 默认使用固定长度隔离变量：
>    - Short：256 input / 128 output tokens；
>    - Long：2048 input / 128 output tokens；
>    - Mixed：80% Short + 20% Long；
>    - 如果 Gate 0 的硬件无法支持，必须在 Pilot 前统一缩短并冻结，不能在 A/B 中改变；
> 2. 使用指数分布 inter-arrival 生成 **3 条不同 seed 的固定 trace**；
> 3. 对同一条 trace 成对重放两个 Token Budget，A/B 顺序交叉或随机，不能三次都先 A 后 B；
> 4. 先用低负载基线生成实验性 SLO，并明确它不是生产业务 SLO；例如按低负载基线的 P90 乘预先固定的容忍系数，生成 TTFT / TPOT 阈值；
> 5. Pilot 定义基线容量 `R_cap`：满足预定 SLO 且失败率低于预定阈值的最高请求率；低载取约 `0.4 × R_cap`，近饱和取约 `0.8—0.9 × R_cap`；
> 6. 正式矩阵：
>
>    ```text
>    2 个负载水平
>    × 2 个 Token Budget
>    × 3 条固定 seed trace
>    = 12 次正式运行
>    ```
>
> 7. Short-only / Long-only 各做一次机制参照；
> 8. 对齐客户端指标和 `/metrics`。
>
> 过载点、Prefix Cache、Chunked Prefill 开关均为可选。

> [!example] 正式运行协议
>
> 在 A/B 前冻结：
>
> | 项目 | 规则 |
> |---|---|
> | Warm-up | 独立执行，不进入正式样本 |
> | 最低发出请求数 `N_min` | 默认 300；按已发出请求计数，不为凑成功样本追加 |
> | 最低发流时间 `T_min` | 默认 180 秒 |
> | 目标请求数 | `max(N_min, ceil(request_rate × T_min))` |
> | 最大发流时间 | 默认 1200 秒或预先固定的等价 GPU 成本 |
> | 单请求 timeout | Pilot 后固定，A/B 中不改变 |
> | 最大在途请求 | 发流前固定，记录触发次数 |
> | Drain | 停止发流后单独等待，超时请求计入失败 |
> | 终止条件 | OOM、服务崩溃、达到发流/成本上限；分别记录 |
>
> 成功、失败、请求超时都进入终态计数；只有成功请求进入延迟分位数。`/metrics` 抓取是低频趋势数据，不能伪装成逐 Engine Step 数据。

> [!example] 4 小时测试与报告
>
> - 每轮独立计算分位数；
> - 报告三轮中位数与 `[min, max]`；
> - 成功样本不足 1000 时，不把 P99 作为正式 A/B 结论；可以在附录展示经验 P99，但必须标记尾部样本不足；
> - 三条 trace 是三个不同 seed；“固定 trace”只用于保证每组 A/B 成对公平，不代表三次使用同一到达序列；
> - 保留一个不符合预期的结果；
> - 写明不能仅靠端到端指标证明的机制。

> [!success] 达标标准
>
> - 有预先写下的假设、SLO、样本和中止规则；
> - Mixed workload 是同一时间线真实混合到达；
> - 能解释“现象”和“原因证据”的区别；
> - 本周末开始投递 Serving / 推理性能实习。

---

## 第 5 周：CUDA 执行模型与 Reduction

> [!note] 目标
>
> 补齐 GPU 访存、同步和 Reduction 基础，为一个深挖 Softmax 做准备。

> [!info] 学习资料
>
> - [CUDA Programming Guide](https://docs.nvidia.com/cuda/cuda-programming-guide/index.html)
> - [CUDA Best Practices Guide](https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/index.html)
> - GPU MODE Lecture 3、4、8、9。

> [!example] 11 小时主线
>
> 1. Vector Add 限时 1 小时；
> 2. Reduction 实现正确基线和 **一个**优化版；根据输入规模选择 Shared Memory 或 Warp Shuffle，不为凑版本数同时实现；
> 3. 以 FP32 为主；
> 4. 使用 CUDA Event；
> 5. 用 CMake 构建；
> 6. 使用 `compute-sanitizer` 检查错误。

> [!example] 4 小时测试与报告
>
> - 极小、非 2 的幂、大输入；
> - 每版先正确再计时；
> - 输出访存模式与线程组织图；
> - 记录一个优化后反而变慢的 Shape。

> [!success] 达标标准
>
> - 能解释 `__syncthreads()`、合并访存、Bank Conflict、Warp Divergence；
> - 能解释 Occupancy 不是最终性能；
> - 不做 FP16、向量化和更多版本，除非核心全部完成。

---

## 第 6 周：CUDA Softmax 与 Profiler 闭环

> [!note] 目标
>
> 完成唯一一个必做 Kernel 项目：
>
> ```text
> 正确性 → 基线 → Profile → 假设 → 修改 → 复测 → 解释回退
> ```

> [!example] 11 小时主线
>
> 1. Row-wise Softmax V0：正确基线；
> 2. V1：一个 Block 处理一行的优化版；根据行宽使用 Warp 内 Shuffle，并在多 Warp 时使用 Shared Memory 汇总，按数据流说明而不是人为拆成两个重复版本；
> 3. CUDA Event 测量；
> 4. Nsight Systems 看 Launch 与时间线；
> 5. Nsight Compute 可用时只看目标 Kernel；不可用时执行 Gate 0 中的替代方案。

> [!example] 4 小时测试与报告
>
> - FP32 必做，FP16 可选；
> - Shape 使用 `(num_rows, hidden_size)`，覆盖少行/多行，以及 1024、1536、2048、3072、4096 等 2 的幂与非 2 的幂行宽；
> - 核心测试只要求 contiguous 输入，其他 layout 可选；
> - 固定编译配置、warm-up、重复次数和误差容限；
> - 极值与数值稳定性测试；
> - 每次只解释 3—5 个与假设相关的指标；
> - 公布慢于 PyTorch / 库实现的区间。

> [!success] 达标标准
>
> - 至少一个“Profiler 证据 → 代码变化 → 指标变化”闭环；
> - 能区分 Kernel latency 与端到端延迟；
> - 不能声称自己已具备 Attention / GEMM Kernel 能力；
> - Triton 和 Custom Op 均不阻塞本周验收。

---

## 第 7 周：vLLM 源码调用链

> [!note] 目标
>
> 从请求跟到 GPU 执行，并进入一个 Attention Backend。

> [!info] 学习资料
>
> - [vLLM Architecture Overview](https://docs.vllm.ai/en/stable/design/arch_overview/)
> - [vLLM V1 Guide](https://docs.vllm.ai/en/stable/usage/v1_guide/)
> - 固定 commit 的 Scheduler、KV Cache、Model Runner、Attention Backend 源码及相邻测试。

> [!example] 11 小时主线
>
> 只选择 **一个文本 Decoder 模型、一个 Attention Backend、单卡、无 Speculative Decoding、无多模态** 的路径，按固定 commit 跟踪：
>
> ```text
> API Server
> → Engine Core
> → Scheduler
> → KV Cache Manager
> → GPU Worker
> → Model Runner
> → 一个 Attention Backend
> ```
>
> 必须回答：
>
> - 哪个进程处理 HTTP，哪个进程调度；
> - Token Budget 在哪里生效；
> - KV Block 何时申请、复用和释放；
> - Model Runner 如何准备输入，以及本配置是否使用 CUDA Graph；不要求通读 CUDA Graph 子系统；
> - Attention metadata、KV Cache 和后端调用如何连接；
> - 哪些代码是 Python，哪些落入 C++ / CUDA / 外部库。

> [!example] 4 小时验证与文档
>
> - 画进程边界和关键类图；
> - 记录实际文件、类、函数和 commit；
> - 使用断点、结构化调试日志或小输入验证调用链；
> - 阅读至少两个相邻单测。

> [!success] 达标标准
>
> - 能在 10 分钟内从 API 讲到 Attention Backend；
> - 不靠背文件名，能解释每层职责；
> - 核对下一周预定义观测 patch 所需字段在固定 commit 中的真实来源；
> - 若源码仍无法运行，第 8 周不得直接做大改动。

---

## 第 8 周：引擎源码改动与测试

> [!note] 目标
>
> 保底任务从计划开始时就固定为：
>
> > 为固定 commit 的 Engine Core / Scheduler 路径增加一个默认关闭的结构化 Engine Step trace，并提供 accounting 单测、输出一致性验证和开销报告。
>
> 可复现 bugfix、缺失回归测试或小型策略改动只做加分项，不能把第 8 周前半周耗在开放式“找问题”上，也不能强行“优化 Scheduler”。

> [!example] 11 小时主线
>
> 改动要求：
>
> - 先审计现有功能和指标，避免重复造轮子；
> - 单一职责、最小 diff、无无关重构、可独立测试和回滚；
> - 使用显式配置开关；关闭时不创建文件、缓冲区或额外调度数据路径；
> - 不使用散乱 `print`；
> - 输出 JSONL 或等价稳定 schema，使用缓冲写入并定义 flush / shutdown 行为；
> - 本地至少运行目标测试、直接相关测试、格式和静态检查；提交 PR 时以项目 CI 为最终约束；
> - 保存 patch、测试命令和设计说明。
>
> 保底 schema 至少包含：
>
> - `run_id`；
> - `step_seq`；
> - Engine Core 进程内的 monotonic timestamp；
> - running / waiting request 数；
> - total scheduled tokens；
> - scheduled prefill tokens；
> - scheduled decode tokens。
>
> 语义限制：
>
> - 只验证第 7 周固定的单卡文本模型、单 backend、无 Speculative Decoding、无多模态路径；
> - Chunked Prefill 下不能把整个 request 简单二分成 Prefill 或 Decode；
> - 每个 request 在本 step 的 Prefill 贡献，应根据“本轮调度 token 数”和“调度前尚未计算的 Prompt token 数”求交集；剩余部分才可计为 Decode，具体字段名以固定 commit 为准；
> - accounting 单测至少检查 `scheduled_prefill + scheduled_decode == scheduled_total`，以及固定 commit 在本限定路径下真实成立的 Token Budget 不变量；
> - 客户端和服务端同机时使用相对 monotonic 时间；跨机时不直接比较 wall-clock，改用 `run_id`、请求/step 标识和明确的时钟误差；
> - Prometheus `/metrics` 是较低频 scrape，只用于趋势，不与 per-step trace 强行逐点相关。

> [!example] 4 小时验证与报告
>
> - 功能正确性；
> - 固定 greedy 输入下，开关前后生成 token 完全一致；
> - trace disabled 路径、schema、step sequence、accounting 和 shutdown flush 测试；
> - 代表负载下 paired on/off 开销报告；不预设任意单次百分比阈值，若开销不可接受则降低采样频率或优化缓冲；
> - 为什么把改动放在该层，而不是 API / Worker / Kernel。

> [!success] 达标标准
>
> - patch 可应用；
> - 测试可复现；
> - 能解释数据所在进程、生命周期和开销；
> - 第 8 周内没有完成行为改动不算失败；
> - 如果只是观测改动，简历必须写“引擎可观测性”，不能写“调度优化”。

---

## 第 9 周：内部证据复验与自动回归

> [!note] 目标
>
> 把第 4 周的外部现象和第 8 周的内部证据连接起来。

> [!example] 11 小时主线
>
> 1. 选择第 4 周最关键的一个近饱和 A/B：
>
>    ```text
>    2 个 Token Budget
>    × 第 4 周的 3 条 seed trace 成对重放
>    = 6 次正式复验
>    ```
>
> 2. 对齐 TTFT / TPOT / Goodput 与 scheduled tokens、queue、KV 指标；
> 3. 对观测开关做一个小型代表负载的 3 组 paired on/off 开销测试；
> 4. 将回归分成两类：
>    - CI smoke：服务启动、小负载成功、字段/schema 完整、greedy 输出一致、trace accounting 正确；
>    - 性能 harness：重复运行后报告 median 和范围，不用单次运行或“数量级回退”作为合格阈值；
> 5. 自动回归入口至少检查：
>    - 服务能启动；
>    - 小负载输出成功；
>    - 原始结果字段完整；
>    - patch 开关前后结果语义一致；
>    - paired benchmark 结果可以被统一解析和比较。

> [!example] 4 小时报告
>
> - 修正或保留第 4 周假设；
> - 报告观测开销；
> - 写出适用硬件、模型、负载和版本边界；
> - 形成 5 分钟和 15 分钟项目讲稿。

> [!info] 条件分支
>
> - **只有 Gate 0 已确认两张同型号 GPU 且核心任务提前完成**：缓冲时间比较 TP=1 / TP=2，并用 Nsight Systems 观察固定模型实际出现的 Collective；
> - **否则**：只输出进程拓扑和可能的 AllReduce / AllGather / Reduce-Scatter 等通信作用，最终以固定模型和框架实际路径为准；禁止声称做过多卡实测。

> [!success] 达标标准
>
> - 每个结论可追溯到原始数据和 commit；
> - 内部指标至少支持或否定一个外部推断；
> - 有一个自动回归入口；
> - 第 9 周末扩大投递。

---

## 第 10 周：面试验收、作品集与集中投递

> [!note] 目标
>
> 停止扩技术栈，验证自己能否通过真实追问。

> [!example] 核心任务
>
> 1. 对前 8 周已持续更新的仓库和 README 做最终裁剪；第 10 周不承担补写全部文档；
> 2. README 包含：
>    - 问题；
>    - 架构；
>    - 版本与复现；
>    - 实验；
>    - 源码改动；
>    - 测试；
>    - 失败与局限；
> 3. 完成一次 60 分钟项目综合模拟面试：Transformer + vLLM + CUDA + 项目反例；
> 4. 修正讲不清的部分；
> 5. 集中投递。

> [!success] 最终验收
>
> - 能在 20—30 分钟内写出带 Shape 注释的 GQA / KV Cache 核心伪代码或最小 PyTorch 实现，并解释 Cache append 与 position offset；
> - 能解释 TTFT、TPOT、Goodput 和开放环负载；
> - 能讲清 Scheduler、KV Cache Manager、Model Runner、Attention Backend 边界；
> - 能展示一个带测试的 vLLM patch；
> - 能展示一个 CUDA Profiler 闭环；
> - 累计完成至少 18 道独立算法题记录、9 次隔日/隔周复做；目标值为 24 / 12，但不以低质量凑数；
> - 在第 6、8、10 周完成 3 次不同侧重的限时编码或模拟面试；
> - 不把未做过的多卡、Kernel、MLOps 或生产经验写进简历。

---

## 贯穿 9 周的面试线

每周固定 5 小时。

| 周次 | 3 小时算法 | 2 小时 C++ / 系统 |
|---|---|---|
| 第 2 周 | 数组、哈希、滑动窗口 | RAII、拷贝/移动、智能指针 |
| 第 3 周 | 栈、队列、堆 | 进程、线程、协程、同步/异步 |
| 第 4 周 | 二分、区间 | 排队、背压、超时、HTTP Streaming |
| 第 5 周 | 链表、树遍历 | Cache、虚拟内存、分页 |
| 第 6 周 | DFS、BFS；45 分钟限时编码模拟 | mutex、condition variable、atomic |
| 第 7 周 | 堆、调度题 | 多进程、IPC、共享内存、序列化 |
| 第 8 周 | 图、拓扑排序 | 编译、链接、CMake、gdb、sanitizer；45 分钟 C++ / 系统模拟 |
| 第 9 周 | 基础 DP、错题 | TP / PP / DP、NCCL Collective |
| 第 10 周 | 2 道未见题限时编码 + 高频错题 | 60 分钟项目综合模拟 |

> [!example] 每周算法最低要求
>
> - 2 道独立完成的新题；
> - 1 道旧题盲写；
> - 写复杂度；
> - 至少 1 道新题限时；若本周完成完整模拟，则可替代；
> - 只看题解不计数。

第 6、10 周模拟中的未见题若独立完成，可以计入当周新题，不重复计数。

第三道新题和第二道复做只使用缓冲时间。

> [!example] 每周系统线至少完成
>
> - 5 个不看资料的口述问题；
> - 1 个 20 分钟以内的 C++ / 系统小编码或伪代码题；
> - 一页错因与边界笔记，并在隔周重新口述。

如果目标 JD 明确强调现代 C++ / 并发，使用第 3、6 周的系统时间分两次完成一个最小 bounded producer-consumer queue，包含关闭语义、虚假唤醒处理和基本测试；它不升级为独立主项目。

如果连续两周无法完成，减少项目加分项，不能削减算法和系统基础。

---

## 止损与删减规则

> [!warning] 按以下顺序删除
>
> ```text
> Triton 自写算子
> → PyTorch Custom Op
> → Prefix Cache / 量化实验
> → 多卡实验
> → 上游 PR
> → 额外正式运行
> ```

> [!success] 不能删除
>
> ```text
> 算法和 C++ / 系统基础
> → 正确性测试
> → 固定版本与复现
> → Serving 主实验
> → 一个 CUDA Profiler 闭环
> → vLLM 调用链
> → 一个带测试的源码改动
> → 内部指标复验
> ```

> [!info] 连续延期时
>
> - 第 4 周没完成 Serving v1：停止新增 CUDA 资料，先完成报告；
> - 第 6 周没完成 Softmax：删除全部 Triton / Custom Op；
> - 第 7 周没跑通源码：第 8 周只做 Python 层小改动；
> - 第 8 周找不到合适功能改动：退回结构化观测 + 测试，不做大策略改造；
> - 第 9 周没有多卡：删除多卡实测，不租卡硬凑结果；
> - 第 10 周禁止新增框架。

---

## 最终求职定位

完成本终审版后，合理定位是：

> **大模型推理性能 / Serving Benchmark / vLLM Runtime 实习候选人：理解 Transformer 推理与 KV Cache，能用 vLLM 构造规范在线负载并分析 Scheduler / Token Budget，能跟踪请求至 Model Runner 和 Attention Backend，完成过带测试的引擎源码改动，并具备 CUDA Kernel 与 Profiler 基础。**

> [!warning] 不应宣称
>
> - 精通推理引擎；
> - 精通 CUDA / Triton；
> - 具备多机多卡生产经验；
> - 做过 PagedAttention / FlashAttention 研发；
> - 具备生产级 MLOps 经验。
